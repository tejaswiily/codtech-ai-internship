# Task 4 – Text Generation Model

This task uses a pretrained GPT-based model to generate coherent text from a user prompt.

## How to Run

1. Install dependencies:
   pip install -r requirements.txt

2. Run:
   python text_generator.py

3. Enter any prompt and the model will generate text.

## Libraries Used
- transformers
