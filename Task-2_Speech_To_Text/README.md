# Task 2 – Speech to Text System

This task converts audio speech to text using Python and SpeechRecognition library.

## How to Use
1. Install dependencies:
   pip install -r requirements.txt

2. Run:
   python speech_to_text.py

3. Choose an audio file (.wav) and the script will print the text.

## Libraries Used
- SpeechRecognition
- PyAudio (if using microphone)
